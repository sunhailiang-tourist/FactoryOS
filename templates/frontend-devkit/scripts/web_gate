#!/usr/bin/env bash
# WEB Gate 入口 → web_gate_cli.py
set -euo pipefail
APP_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$APP_ROOT"
exec "$APP_ROOT/scripts/py.sh" "$APP_ROOT/scripts/web_gate_cli.py" "$@"
