---
description: WebDev 工作流 — 用户发送【WebDev模式启动】时加载 · 独立步步流 L4
alwaysApply: false
---

# web-admin Dev 工作流 v1.1（独立 · 可迁出 · L4）

> 细则：`WEB-STEP0.md` · `WEB-DEV-GATES.md` · `WEB-GATES.md` · `WEB-REDLINES.md`  
> 绝对门禁：`WEB-00` · `WEB-01`

## 激活

`【WebDev模式启动】` 后先定类型：

| 类型 | 下一步 |
|------|--------|
| **新功能** | 材料准入 → `材料已齐` → `./scripts/web_gate materials` → Step0 |
| Bug / 联调 | `./scripts/web_gate materials --na --reason '…'` → Step0 |

## 防误执行

未 `可以开始`+`web_gate start` 前：**禁止** 改 `src/**` 业务。  
越权/结构 → 停机 → `确认越权` / `确认结构变更`。

## Step 0 → `可以继续`

见 `WEB-STEP0.md`。

## 规划 → `确认规划`

落盘：`_web_pipeline/<date>/plan/plan-*.md`（模板 v2：Step 总览 + ### Step N）  
然后：`./scripts/web_gate plan --plan …`（写 plan.ok）

## 每 Step N

1. Test failing → `./scripts/web_gate test`
2. 用户 `可以开始` → `./scripts/web_gate start --step N`
3. 仅本 Step · 10 项自检 · **运行时证据**
4. step-stop → WebTest → WebVerify → `./scripts/web_gate step --step N`

## 交付

`activate.sh` 绿；改门禁后 `./scripts/web_gate harness-eval`；用户 `可以提交` 后才 commit。

## 禁止

跳 Step · 超 plan · 伪造 `_web_pipeline/.gates` · 用 `_factoryos_pipeline` 代替 · Test 偷改业务
