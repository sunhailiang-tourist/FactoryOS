# 预开发说明：<标题>

> **唯一执行清单**：`确认规划` 后，编码**只按本文各 Step 落地**；禁止超范围、禁止跳 Step。  
> 呈现原则：**精简表格**，开发者一眼看清——分几步、每步做什么、落哪、是否合理、怎么用、入参/出参。

- **日期**：YYYY-MM-DD
- **类型**：新功能 / Bug / 联调 / …
- **材料准入**：`materials-*.md` 路径（新功能必填）或 `N/A`
- **对照契约**：Contract Registry（published）+ `contracts/openapi/` · `contracts/acceptance/`
- **机械门禁**：`./scripts/gate plan` 会跑 `check_plan_spec`（§4/§6 结构 · 新功能 materials · UI §8）

---

## 1. 迭代目标

| 项 | 内容 |
|----|------|
| 一句话 | |
| 解决什么问题 | |
| 可测验收（2～4 条） | 1. … 2. … |

---

## 2. AC 对账表（必填）

| AC ID | 标题 | 本迭代 | 验证方式 |
|-------|------|--------|----------|
| G-01 | | 是/否 | pytest -k 'G-01' |

---

## 3. 红线对账（必填）

| 红线 | 涉及 Step | 负向测试 |
|------|-----------|----------|
| R-02 | | E-03 |

---

## 4. Step 总览（必填 · 一眼看全部分步）

| Step | 名称 | 功能一句话 | 解决什么问题 | 接口（新增/改） | 新增函数 | 新增文件 | 改动文件数 | Harness |
|------|------|------------|--------------|-----------------|----------|----------|------------|---------|
| 1 | | | | METHOD /v1/… | `fn_a` | 是/否 | N | `gate step --step 1 -k '…'` |
| 2 | | | | | | | | |

**本需求共 N 步。** 下列 §6 起按 Step **逐张展开**；实现时严格按表序，一步一停机。

---

## 5. 全局接口清单

| 方法 | 路径 | 新增/修改 | 归属 Step | 用途 |
|------|------|-----------|-----------|------|
| | | | | |

---

## 6. 分步详表（每 Step 一节 · 结构固定 · 禁止省略列）

### Step 1 — <名>

#### 6.1 本步摘要

| 项 | 内容 |
|----|------|
| 功能是什么 | |
| 解决什么问题 | |
| AC ID | |
| 可测验收（一句话） | |
| 风险 | 幂等/越权/shadow/无 |

#### 6.2 接口

| 方法 | 路径 | 新增/修改 | 说明 |
|------|------|-----------|------|
| | | | |

#### 6.3 函数（新增 / 修改）

| 符号 | 文件路径 | 新增/修改 | 职责一句话 | 上游调用方 | 下游被调 |
|------|----------|-----------|------------|------------|----------|
| `fn_name` | `src/server/os_core/…` | 新增 | | | |

#### 6.4 文件落位

| 路径 | 新增/修改 | 为何落这里（依赖方向 · 1 句） | 是否合理 | 有无重复可复用 |
|------|-----------|------------------------------|----------|----------------|
| | | | 是/否（否须改方案） | 无 / 有→复用 `…` |

#### 6.5 怎么用（调用方式）

| 入口 | 示例 |
|------|------|
| HTTP | `METHOD /v1/…` + 鉴权头 |
| 代码 | `from … import …; …` |

#### 6.6 入参数据结构

```json
{
  "<field>": "<type · 必填/可空 · 语义>"
}
```

| 字段 | 类型 | 必填 | 语义 |
|------|------|------|------|
| | | | |

#### 6.7 返回数据结构

```json
{
  "<field>": "<type · 语义>"
}
```

| 字段 | 类型 | 语义 |
|------|------|------|
| | | |

#### 6.8 本步验收盘

```bash
./scripts/gate step --step 1 -k '<AC-ID>'
```

---

### Step 2 — <名>

（同 Step 1 结构：6.1～6.8；按需复制，**禁止**只写总览不展开。）

---

## 7. Harness 验收盘（全局）

```bash
./scripts/gate step --step N -k 'G-01'
./scripts/gate delivery   # 全部 Step 通过后
```

---

## 8. UI / 原型字段对账（命中强制 · 未命中写 N/A）

> 服务端开/改接口契约 + 有需求图时强制；core/纯前端/无图 → N/A。详见 `STEP0.md` §UI 对账。

- **命中判定**：是 / 否（否 → 理由：________；下表可删）
- **图片材料清单**：（或链到 Step 0 / materials 落盘路径）

### 8.1 界面字段表

| 视图ID | 图/页码/位置 | UI元素/文案 | 字段标识 | 数据来源 | 归属接口 |
|--------|--------------|-------------|----------|----------|----------|
| | | | | | |

### 8.2 接口字段对账表（接口级覆盖 · `可以开始` 前全闭环）

| UI字段 | 最终接口字段路径 | Schema/DTO | 实现落点 | 状态 |
|--------|------------------|------------|----------|------|
| | 如 `data.items[].name` | | `os_core/...` 或 `api/...` | 已实现 / 不需要（理由） |

**硬失败**：任一行状态为 `待实现` / `未知` / `未确认` → plan 不合格，禁止 `可以开始`。  
**状态仅允许**：`已实现` | `不需要`（必须写业务理由）。

---

## 9. 规划自检（落盘前勾选）

- [ ] §4 总览步数 = §6 展开节数
- [ ] 每 Step 含：函数表 · 文件落位（合理+重复）· 怎么用 · 入参 · 返回
- [ ] api 仅路由；业务在 os_core；无超 plan 范围
- [ ] 新功能已过材料准入
- [ ] UI 对账：未命中已写 N/A，或命中时 §8.1/§8.2 无「待实现/未知」
