# 办公自动化连接器（预留）

首期 **不实现** 传统 OA（泛微/致远）读写。Phase 2 起只读+通知；Phase 3 审批写。

IM 入口（钉钉/企微）见 `conn-dingtalk` / `conn-wecom`，可与 OA 协同 Pack 组合。

接入时在此目录下创建 `{vendor}/samples/`。

Pack ID：`conn-oa-read` / `conn-oa-write`
