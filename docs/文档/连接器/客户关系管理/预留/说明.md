# 客户关系管理连接器（预留）

**Customer Relationship Management** — 客户、商机、销售订单、交期承诺。

| 阶段 | 能力 |
|------|------|
| Phase 2+ | L0 只读（订单/客户查询，参与 Graph 追溯） |
| Phase 3+ | L1 建议 / L2 写（选配，单独 Pack） |

Pack：`conn-crm-{vendor}-read` / `conn-crm-{vendor}-write`

接入时在 `{vendor}/samples/` 存放 API 样例。
