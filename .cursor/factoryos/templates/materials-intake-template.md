# 材料准入单：<本轮标题>

> **绝对门禁**：类型为「新功能」时，本单未齐 + 用户未确认前，**禁止**进入 Step 0（需求分析）。  
> 真源：`.cursor/factoryos/STEP0.md` §材料准入 · `factoryos-dev-workflow.mdc`

- **日期**：YYYY-MM-DD
- **口令**：`【Dev模式启动】`
- **类型**：新功能（本门禁仅此类型强制）

## 1. 功能需求文案（必填）

| 项 | 内容 |
|----|------|
| 一句话目标 | |
| 文案位置 | 仓库路径 / 粘贴摘要 / PM 落盘路径（`pm/`） |
| 验收口径（可测） | 2～4 条 |

**硬失败**：无文案路径且无可用摘要 → 不得进入 Step 0。

## 2. 需求资料（必填 · 至少 1 项有效）

| # | 资料类型 | 路径或链接 | 用途 |
|---|----------|------------|------|
| 1 | PRD / 说明 / AC / 原型图 / Figma / 其他 | | |

**硬失败**：表为空或均为无效路径 → 不得进入 Step 0。

## 3. 补充需求资料（强制追问 · 用户必须答）

Agent 必须追问：「是否还有补充需求资料？」

| 用户答复 | 记录 |
|----------|------|
| 有 | 下列补充项 |
| 无补充 | 用户原话或口令：`无补充` |

### 补充清单（若有）

| # | 资料 | 路径/说明 |
|---|------|-----------|
| | | |

## 4. 准入结论

- [ ] §1 文案已齐
- [ ] §2 资料已齐
- [ ] §3 已追问且用户已答（有补充已登记 / 或确认无补充）
- [ ] 用户口令：`材料已齐`（或等价明确确认）

**仅当以上全勾选** → 用户说 `材料已齐` → 执行：

```bash
# 新功能
./scripts/gate materials --materials _factoryos_pipeline/<YYYY-MM-DD>/plan/materials-<HHmm>-<slug>.md
# Bug / 联调
./scripts/gate materials --na --reason 'Bug修复'
```

写入 `materials.ok` 后，才允许进入 Step 0（0-A/0-DB/0-B）与写 `plan-*.md`。

落盘：`_factoryos_pipeline/<YYYY-MM-DD>/plan/materials-<HHmm>-<slug>.md`（或本轮 plan 附录引用本路径）
