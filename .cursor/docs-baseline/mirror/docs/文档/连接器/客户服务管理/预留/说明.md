# 客户服务管理连接器（预留）

**Customer Service Management** — 售后服务、工单、客诉、保修。

> 若客户语境为 **SCM（供应链）**，由 `erp` + `wms` + `aps` 覆盖，不使用 `csm` 枚举。

| 阶段 | 能力 |
|------|------|
| Phase 2+ | L0 只读（服务单/客诉状态） |
| Phase 3+ | L2 写（选配，服务闭环） |

Pack：`conn-csm-{vendor}-read` / `conn-csm-{vendor}-write`
