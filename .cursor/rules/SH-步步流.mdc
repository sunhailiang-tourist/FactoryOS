---
description: SH-步步流 v2 总纲 — L1人机轨 + Spec + Harness · 步步确认 · contracts/ 真源
alwaysApply: true
---

# SH-步步流 v2（FactoryOS 研发总纲）

**真源**：`.cursor/factoryos/INDEX.md` · `contracts/` · `REDLINES.md` · [ACTIVATION.md](../factoryos/ACTIVATION.md)

## 三层控制

| 层 | 作用 |
|----|------|
| **L1 人机轨** | 关键词闸门；每 Step 停机等你确认 |
| **L2 Spec 轨** | `contracts/` + plan 内 AC ID + 红线对账 |
| **L3 Harness 轨** | failing test（红）→ 实现（绿）→ 按改动面 L0–L2 + 停机 L0–L3 全绿 |

## 关键词（缺一不可）

`可以继续` → `确认规划` → `可以开始` →（每 Step）`可以继续` · 阻断时 `风险接受并继续` · 失败时 `测试不通过`

## 落盘（反黑盒）

`_factoryos_pipeline/<YYYY-MM-DD>/{plan,test,step-stop,verify,summary}/` — 聊天不能代替文件

## 三 Agent

- **Dev**：`【Dev模式启动】` → `.cursor/rules/factoryos-dev-workflow.mdc` · 细则 `STEP0.md` `DEV-GATES.md`
- **Test**：`【Test模式启动】` → `.cursor/rules/factoryos-test-workflow.mdc` · 细则 `TEST-GATES.md`
- **Verify**：`【Verify回合】Step N`（**新对话**）→ 细则 `VERIFY-GATES.md` · 只写 `verify/`

Test 只写 `src/tests/**`；禁止改业务实现。

## 单轮节拍

Step0理解 → plan落盘 → test-plan+failing test → 每Step：可以开始→实现→step-stop→Verify新会话→gate step绿→可以继续 → summary交付

## Step 验收盘（未绿禁止「可继续」）

详 [HARNESS-SCRIPTS.md](../factoryos/HARNESS-SCRIPTS.md) · [scripts/README.md](../../scripts/README.md)

```bash
./scripts/gate step --step N -k '<AC-ID>'
```

## 禁止

跳 Step · 超 plan · 无落盘写码 · pytest/CI 未绿宣称通过 · Test 偷改 `os_core`/`apps` 业务
