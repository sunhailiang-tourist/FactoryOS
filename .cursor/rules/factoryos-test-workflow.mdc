---
description: FactoryOS Test Agent v2 — 用户发送【Test模式启动】/【Test·Step N 验收】/【Test·终轮回归】时加载
alwaysApply: false
---

# FactoryOS Test 工作流 v2.1

> 参考 `rules/testing-expert-*.mdc` **纪律层**；绑定 `contracts/acceptance/` · **pytest 为主**。  
> 细则：**`.cursor/factoryos/TEST-GATES.md`** · 模板 **`templates/`**

## 激活（三档 · 不可混用省略）

| 口令 | 时机 |
|------|------|
| `【Test模式启动】` + plan | 编码前 · failing tests |
| `【Test·Step N 验收】` | 每 Step · Dev step-stop 后 · **Verify 前** |
| `【Test·终轮回归】` | 全部 Step 完成后 · **commit 前** |

## 硬隔离

- **只写/改** `src/tests/**` 与 test-plan / step-regression / final-regression / bug 落盘
- **禁止** 改 `src/os_core/**`、`src/apps/**` 业务实现
- Bug → 报告（`templates/bug-template.md`）+ 补测试，不偷修

## 前置 Gate A–G（编码前回合不可跳过）

详见 `TEST-GATES.md`。

## Step 0 落盘（编码前）

`_factoryos_pipeline/<date>/test/test-<HHmm>-<slug>.md`  
模板：`templates/test-template.md` → `gate test`

## 每 Step 验收（强制）

1. 读 plan Step N · step-stop · **`git diff` 本步改动面**
2. 执行本步 **硬性验收计划**（AC · pytest · 落位表）
3. 落盘 `test/test-<HHmm>-stepN-regression.md`（`templates/test-step-regression-template.md`）
4. 结论：通过 / 需改进 / 阻断 → 通过后 Dev 才进入 Verify

```bash
./scripts/gate step --step N -k '<AC-ID>'
```

## 终轮回归（commit 前强制）

1. 全量 `git diff` · 本轮全部 AC · 存量 workflow/contract/integration
2. 落盘 `test/test-<HHmm>-final-regression.md`（`templates/test-final-regression-template.md`）
3. `./scripts/gate delivery` 绿后，才允许提示 commit

## 阻断

触及存量链路 · 评估「阻断」→ 须 `风险接受并继续` 才可给合并结论。

## 禁止

跳过单步验收 · 跳过终轮回归 · curl 烟雾替代 pytest · 引用 `ai-elephant-enlighten-api` 脚本
