---
description: 编码绝对门禁——中文注释、README、无冗余；用户确认前禁止写 os_core/apps 业务代码
alwaysApply: true
---

# 编码绝对门禁（强制）

**真源**：`.cursor/factoryos/SH-步步流.md` v2 · `.cursor/factoryos/GATES.md` · Contract Registry + `contracts/` export（ADR-008）

## 开工禁令

- 用户确认 **「确认编码门禁，开始 W1」** 前：**禁止** 创建 `src/server/os_core/**/*.py`（`__init__.py` 与 W1 工程底座除外）、`src/server/api/**/*.py` 等业务实现。
- 每个 **项目根** 必须先有 `README.md`（见编码绝对门禁 §2），再允许实现文件。

## 中文注释（每个源文件）

1. **文件头**：作用 · 业务关联 · 上游 · 下游 · 关联文档（中文）。
2. **函数**：功能 · 业务含义 · 上游调用方 · 下游被调方 · 参数/返回业务说明。
3. **字段**：含义 · 业务含义 · 业务关联（Pydantic/SQLAlchemy/TS 类型字段均须）。
4. 目标：**读代码像读业务流程**，不依赖 ADR 才能懂。

## 代码质量

- **禁止** 重复逻辑：≥2 处相同必须抽取；禁止在 `src/server/api` 重复 `os_core` 已有能力。
- `src/server/api` **仅** 路由/依赖注入/校验，无业务规则。
- PR 必须过：`./scripts/gate pr`（内含 harness full + 静态 + 追溯门禁）。

## 与架构红线并存

ADR-002 写路径、Agent 禁写、integration 公开 API 等 **不因注释而放宽**。

## 新模块检查

新建 `src/server/os_core/*` 或 `src/server/api/*` 目录 → 先写该目录 `README.md`（是什么/功能/不负责/上下游/文档链接）。
